#include<iostream>
#include<vector>
using namespace std;
int main()
{
	int t=10,k=0;
	vector <int> arr;
/*cout<<"Enter the numbers (0 to quit) : ";
while(cin>>t&&t){
	arr.push_back(t);
}

cout<<"\nAll the non_zero elements entered by you are : ";
 t=arr.size();
// cout<<arr.size();
while(cout<<arr[k++]<<" "&&(!(k==t)))
{}


*/
arr.push_back(1);
arr.push_back(1);
arr.push_back(1);
arr.push_back(1);
arr.push_back(1);
arr.push_back(1);
arr.push_back(1);
arr.push_back(1);
cout<<arr.size();

	return 0;
}